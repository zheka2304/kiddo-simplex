sceneType: Generic
generatingFunc: 'task: simplex_task5_1'
initialScript: |-
  import player
  import console

  # Add your code here
taskDescription: |-
  Ты — заскучавший попугай, зайди в телетайп и проведи переписку с Кадаброй. На вход тебе подаются несколько сообщений, в каждом
  тебе в нужно заменить часть симоволов и сокрашений на соответствующие знаки припинания, после чего записать результат в вывод:

  - `"ТЧК"` заменить на `"."`
  - `"ЗПТ"` заменить на `","`
  - `"ВОСКЛИЦАТЕЛЬНЫЙ_ЗНАК"` заменить на `"!"`
  - `"_"` заменить на `" "` (пробел)


  Для замены частей строк, используй метод строки replace('что заменить', 'на что'), он вернет измененную строку,
  оргинальная строка при этом не изменится. Например код ниже заменит все буквы а, на б:
  ```
  s = u"ааа ббб ввв"
  s = s.replace(u"а", u"б") # s == u"ббб ббб ввв"
  ```


