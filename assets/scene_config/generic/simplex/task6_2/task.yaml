sceneType: Generic
generatingFunc: 'task: simplex_task6_2'
initialScript: |-
  import player
  import console

  # Add your code here
taskDescription: |-
  Распакуй и прочитай пустое письмо. Для этого подбери его и положи на стол, чтобы сделать это, используй `player.pickup('letter')` — подобрать предмет под собой
  и `player.place('letter', 0, 1)` — положить перед собой.

  Когда письмо окажется на столе, взаимодействуй с ним через `player.interact()`, чтобы открыть консоль. На вход в консоль
  подается случайное количество строк, которые нужно объединить в одну и разделить переносами строк — `'\n'`. Полученный результат
  нужно записать в вывод консоли, после чего закрыть ее.


